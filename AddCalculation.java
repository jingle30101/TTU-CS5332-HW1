package homework;

public class AddCalculation {
    public int add(int i) {
        int sum = 0;
        //The add() operation adds 10 to the integer in a message and returns the result.
        sum = 10 + i;
        return sum;
    }
}